h2
  | Become a Guru
= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
  = render "devise/shared/error_messages", resource: resource
  .field
    = f.label :name
    br
    = f.text_field :name, autofocus: true, autocomplete: "name"
  .field
    = f.label :email
    br
    = f.email_field :email, autocomplete: "email"
  .field
    = f.label :password
    - if @minimum_password_length
      em
        | (
        = @minimum_password_length
        |  characters minimum)
    br
    = f.password_field :password, autocomplete: "new-password", class: 'password-confirmation-password'
  .field
    = f.label :password_confirmation
    =< octicon 'x-circle', class: 'error-mark hide', fill: 'red'
    =< octicon 'check-circle', class: 'success-mark hide', fill: 'green'
    br
    = f.password_field :password_confirmation, autocomplete: "new-password", class: 'password-confirmation-check'
  .actions
    = f.submit "Sign up"
= render "devise/shared/links"
