= form_with url: sessions_path, local: true do |form|
  p
    = form.label :email
    = form.text_field :email

  p
    = form.label :password
    = form.password_field :password

  p = form.submit 'Enter'
